name: Build & Push to GHCR

on:
  push:
    branches:
      - deploy

jobs:
  build-push:
    uses: sward-UPC/.github/.github/workflow-templates/build-push-ghcr.yml@main
    with:
      image_name: sward-lambda-alertas
      needs_shared: false
    secrets: inherit
